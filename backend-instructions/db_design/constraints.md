# Database Constraints

These invariants ensure data integrity at the storage layer:

- `Max word count per chapter generation (approx 3000 words)`
- `Strict input validation to prevent prompt injection attacks`
- `File export size limits (50MB)`
